{% extends 'baseges.html.twig' %}

{% block page %}
    <a class="navbar-brand ps-5" style="text-align: center;" href="index.html">Ajout Burger</a>
 {% endblock page %}







    {% block body %}
    <div id="layoutSidenav_content">
        <main>
            <div class="container-fluid px-4">
                <div class="ml-5" style="margin-left: 14%;color:#ffbe33">
                    <h1 class="mt-4" style="font-size:32px ;">Ajout Burger</h1>
                    <ol class="breadcrumb mb-4">
                        <li class="breadcrumb-item active">Ajout Burger</li>
                    </ol> 
                </div>
               
                    <div class="forms card" style="margin-left: 14%; width: 60%;margin-top: 7%;">
                        {{ form_start(formBurger)}}   
                        <div class="row">
                            <div class="form-controle1 col-6">
                                <label for="">Nom Burger</label>
                                {{ form_widget(formBurger.nom,{'attr':{'placeholder': "Entrer le nom du Burger"}}) }}
                                <div class="form-error">
                                    {{ form_errors(formBurger.nom) }}
                                </div>
                            </div>
                            <div class="form-controle1 col-6">
                                <label for="">Prix Burger</label>
                                {{ form_widget(formBurger.prix,{'attr':{'placeholder': "Entrer le prix du Burger"}}) }}
                                <div class="form-error">
                                    {{ form_errors(formBurger.prix) }}
                                </div>
                            </div>
                        </div>
                            <div class="form-controle1">
                                <label for="">Description</label>
                                {{ form_widget(formBurger.description,{'attr':{'placeholder': "Entrer la description du Burger"}}) }}
                                <div class="form-error">
                                    {{ form_errors(formBurger.description) }}
                                </div>
                            </div> 

                            {% if app.request.attributes.get('_route') == 'modifier_burger' %}
                            {% for image in burger.images %}
                                <img src="{{'/uploads/' ~ image.nom_image}}" style="width: 30%;" alt="">
                            {% endfor %}
                            {% endif %}
                            <div class="imgs">
                                <label for="" class="imglabel">Choisir les images du burger</label>
                                {{ form_widget(formBurger.image,{'attr':{'placeholder': "Entrer l'image du burger"}}) }}
                                <div class="form-error">
                                    {{ form_errors(formBurger.image) }}
                                </div>
                            </div>  
                            <div style="display: flex ; float:right;">
                                <button type="submit" class="ajouter">Ajouter</button>
                                <button type="submit" class="annuler">Annuler</button>
                            </div>
                            
                        {{ form_end(formBurger) }} 
                    </div>
            </div>
        </main>
    </div> 
    {# <main>
        <div class="container-fluid px-4">
            <h1 class="mt-4">Ajout Burger</h1>
                <ol class="breadcrumb mb-4">
                    <li class="breadcrumb-item active">Ajout Burger</li>
                </ol>
            {{ form_start(formBurger)}}   
            {{ form_end(formBurger) }} 
        </div>
    </main> #}   
    
    
      
    {% endblock %}
